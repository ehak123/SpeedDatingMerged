'use strict';

var _defineProperty2 = require('babel-runtime/helpers/defineProperty');

var _defineProperty3 = _interopRequireDefault(_defineProperty2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _ = require('lodash');

module.exports = function (arrays, field) {
  return arrays.reduce(function (previousValue, currentValue) {
    return mergeTwo(previousValue, currentValue, field);
  });
};

function mergeTwo(first, second, field) {
  return _.map(first, function (object) {
    var secondObject = _.find(second, (0, _defineProperty3.default)({}, field, object[field]));
    return _.merge(object, secondObject);
  });
}